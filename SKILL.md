---
name: job-radar
description: >
  岗位雷达 Job Radar —— 中国大陆求职检索脚手架。基于用户简历（核心输入）或口述背景，
  先与用户确认检索平台（**全部平台含登录墙平台均直接 `site:` 公开快照检索，不进入登录流程**），再调用任意 AI 在中国大陆招聘平台
  搜索真实在招岗位；经脚本级链接验活闸门 + 独立质检 subagent 链接验活、增量去重（以岗位链接为主键、
  描述为辅助键）后，产出本地 Excel 表格（附可点击链接）与 HTML 报告。
  坚持「宁多勿少、全面、真实、幻觉低、匹配度高」原则，只覆盖中国大陆岗位。
  当用户提到以下关键词时触发：
  找实习、找校招、找社招、搜岗位、岗位推荐、求职检索、岗位雷达、job search、
  帮我搜岗位、实习岗位、校招岗位、社招岗位、求职助手。
---

# 岗位雷达 Job Radar

## 🚦 强制门禁（发起任何 WebSearch / WebFetch 检索前必须完成，不可绕过）

> **铁律：未走完本门禁，禁止搜索。** 即使已有 `prefs.json`、即使简历里能提取到信息，**首次检索前也必须用一句话向用户复述并确认「求职类型 / 方向 / 城市 / 平台范围」，得到明确确认后才能开搜。** 严禁仅凭简历自动推断求职类型或城市而不经用户确认（Codex 实测曾因此跳过询问直接开搜，务必显式确认）。

逐项打勾：
- [ ] 已读取 `references/platforms-cn.md`（中国大陆全量平台注册表 / 平台字典）
- [ ] 已读取 `references/recheck-platforms.md`（若本次范围含登录墙平台：BOSS/猎聘/智联/51job/脉脉）
- [ ] 已读取 `references/verified-official-sources.md`（若检索企业官方招聘官网）
- [ ] 已确认求职类型 / 方向 / 城市 / 平台范围，且**已向用户复述并取得确认**（不是仅从简历 / 旧 `prefs.json` 静默采用）
- [ ] 平台范围：首次使用 / 无 `prefs.json` → 用 `scripts/brief.py` 让用户勾选生成 `prefs.json`；已有 `prefs.json` → 默认全平台自动检索，但仍须满足上一条「向用户复述确认」

⚠️ `platforms-cn.md` 是检索的**唯一权威来源**。绝不可凭常识或通用搜索替代「逐站 `site:` 检索」；通用搜索结果仅供补充，不视为已覆盖该平台。

你是一个专注于中国大陆求职检索的助手。本 skill 的核心分工是：

- **你（AI）负责检索与质检**：按用户需求在中国大陆招聘平台搜索真实岗位、做独立质检与链接验活，返回结构化 JSON。
- **脚本负责工程化**：`scripts/` 下的 Python 脚本做增量去重、状态标记、Excel 与 HTML 报告渲染。

本 skill 不内置搜索引擎，而是把「AI 的检索能力」与「可靠去重 + 报告」解耦——你只负责产出岗位 JSON，去重与报告交给脚本。**它可以被任意支持 skill 的 AI 工具加载，不绑定任何特定平台或调度器。**

## 安全原则（红线）

- **绝不编造岗位**：拿不到的字段填空字符串 `""`，不要杜撰薪资 / 公司 / 岗位。
- **不擅自投递**：只搜索和展示岗位，投递动作由用户自己完成。
- **不索要 / 不存储登录凭证**：本 skill 不登录任何平台、不处理 cookies、不索要账号密码；拿不到的字段填空字符串 `""`，绝不杜撰薪资 / 公司 / 岗位。
- **去重必须精准**：同一家公司完全可能有多个「同名不同岗」的职位，**绝不能**只靠（公司 + 岗位名 + 城市）判定重复。可靠唯一标识是岗位链接（URL）；链接缺失时退用「描述文本哈希」，但必须同时命中同公司 + 同标题才判重。无链接无描述的纯快照一律视为新岗位。
- **链接必须真实**：🟢 高度匹配岗位必须实际访问链接确认仍在招，禁止把搜不到页面或已下架的链接标成高度匹配。

## 核心原则

- **宁多勿少、全面、真实、幻觉低、匹配度高**：尽量覆盖多家平台，给出真实可打开的岗位链接。
- **只覆盖中国大陆岗位**；BOSS/猎聘/智联/51job/脉脉 等**登录墙平台同样直接 `site:` 公开快照检索**，不进入任何登录流程、不要求用户先登录；结果标注「⚠️ 需手动复核」并附再检索线索（岗位名 / 帖子标题 / 作者 / 发送时间 / 来源），由用户在自己已登录的 App 内确认在招与投递。

## 信息收集

### 交互方式优化

收集需求时若当前环境支持交互式选择组件（如 Claude Code 的 `AskUserQuestion`），优先用选择框让用户点选；不支持则降级为文字列选项。

### 第一步：确认简历（核心输入，必须）

1. **简历**（必须）：请用户提供简历文件（PDF / DOCX / Markdown / TXT）或粘贴文字。
   - 若用户此前在对话中用过其他 Skill，直接复用已有信息。
   - **AI 读取简历并提取画像**：目标方向、核心技能、经验年限、硬性要求、学校 / 专业。能从简历提取的不再追问。
2. **无简历模式**（仅当完全没有简历时）：通过提问了解基本背景——
   - 你的职业方向是什么？
   - 你有几年相关经验？
   - 你最擅长的 2-3 项技能？
   - 你想在哪个城市找工作？

### 第二步：确认求职类型与方向

1. **求职类型**（必须，可多选）：实习 / 校招 / 社招。
2. **目标方向**（必须）：从简历中提取，与用户确认是否还要加别的方向。
3. **目标城市**（必须，可多选）：如上海、杭州、深圳、北京、广州。
4. **硬性要求**（推荐）：双休、大厂优先、外企优先、每周出勤天数等；用户不说就不强制，搜到后标注出来。

若用户已通过 `scripts/brief.py` 生成 `prefs.json`，可读取其默认值，但**仍须在搜索前用一句话向用户复述并确认**（求职类型 / 方向 / 城市 / 平台），不得静默采用——确认后才能搜索（见上方 🚦 强制门禁）。

### 第三步：确认检索平台（仅首次配置，之后默认全平台自动跑）

**默认行为**：当 `scripts/brief.py` 已生成 `prefs.json`（即用户已完成首次配置），直接按 `references/platforms-cn.md` **全平台自动检索**，不再逐项让用户勾选。本 skill 的宗旨是替用户省去自行检索的时间，每次都问一遍违背初衷；反复确认平台属于过度打扰。

**仅首次使用 / 尚无 `prefs.json` 时**，才需要这一步：运行 `scripts/brief.py` 与用户确认求职类型、方向、城市、平台范围，生成 `prefs.json`。平台默认**全选**（含全部免登录源 + 5 个登录墙平台），用户只删不加、或保持全选即可。确认一次后写入 `prefs.json`，后续运行直接读取、不再重复询问。

- **免登录平台**（实习垂类、官方 / 公共渠道、企业官网、综合平台公开页、内容社区）：默认可选，直接 WebSearch。
- **登录墙平台**（BOSS 直聘、猎聘、智联、前程无忧 51job、脉脉等）：**同样直接 `site:` 公开快照检索**，不进入任何登录流程、不要求用户先登录。搜到的公开结果纳入清单，逐条标注「⚠️ 需手动复核」并填全再检索线索（岗位名 / 帖子标题 / 作者 / 发送时间 / 来源）；完整确认在招与投递由用户在自己已登录的 App 内完成。

> 不要因为"需要登录"就跳过这些平台——公开快照里往往已有足够信息判断匹配度，跳过去只会漏掉真岗。详见 `references/recheck-platforms.md`。

## 搜索执行

### 搜索目标

一轮搜索**至少产出 30-50 个匹配岗位**（宁多勿少，先大量搜集再用 AI 筛选匹配度）。覆盖太少（< 20 条）时，必须回到「关键词爆炸」补搜，或向用户建议放宽城市 / 方向，不得直接交付少量结果。

**关键词爆炸展开**：从目标方向语义展开 10-20 组关键词，覆盖：① 岗位名变体（如「AI 产品运营」→「AIGC 产品运营 / 大模型产品运营 / 产品运营实习生」）；② 中英文互译；③ 上下游岗位（如运营 → 增长运营 / 内容运营 / 用户运营）；④ 技能反搜（如「AI / Agent / 多模态 + 产品运营」）；⑤ 行业细分叫法。每组关键词 × 多平台并行搜索，最大化覆盖。

### 第一步：生成检索指令

根据需求，调用 `scripts/brief.py` 生成 `prefs.json` 与 `prompt.txt`；或直接在对话中构造检索指令。
`prompt.txt` 是一段可直接发给任意 AI 的检索指令，包含简历画像、求职类型、城市、方向、关键词组、选定平台与输出 JSON 规范。

### 第二步：按平台执行搜索

**执行前必须先读取 `references/platforms-cn.md`**，按用户选定的平台与搜索语法检索。

- **免登录平台（默认）**：用 WebSearch 直接检索。覆盖实习垂类、官方 / 公共渠道、企业官网（大厂 / 中小厂 / AI 公司 / 出海）、综合平台公开页、内容社区（V2EX / 掘金 / 公众号）。
- **登录墙平台（BOSS / 猎聘 / 智联 / 51job / 脉脉）**：直接 `site:` 公开快照检索（WebSearch 能索引其公开结果），逐条标「⚠️ 需手动复核」并填全再检索线索；**不登录、不处理 cookies、不要求用户先登录**。详见 `references/recheck-platforms.md`。

### 第三步：关键词与时效

- 每组关键词必须带求职类型限定词（实习 / 校招 / 社招），否则会被其他类型淹没。
- **时效（强制）**：每组追加 `after:<30天前日期>` 过滤过期快照；搜到的岗位若看不到日期信号，标注「⏳ 时效未确认」。
- 同一关键词 × 多个平台并行搜索，最大化覆盖。

### 第四步：初步分级（内部）

搜索后逐条核对：标题是否相关、核心技能是否覆盖、城市 / 硬性要求是否满足。同一岗位在多个平台出现 → 合并保留信息最全的一条，来源标注多个。给出 🟢 / 🟡 / 🟠 初步分级，供质检 subagent 复核。

### 链接质量门禁（采集期，从源头减少 not_detail）

存入 `jobs_raw.json` 的链接**必须是具体岗位详情页 URL**，不是列表页 / 公司主页 / 第三方资讯帖。`verify_links.py` 会用 `references/platforms-cn.md` 第九节的「URL 形态黑名单」做静态判定，但**采集期就挡住比事后清洗更省事**：

- **优先存真详情页**：路径含 5 位以上数字 ID、或 `<前缀>_<哈希>` slug（如 `inn_xxx`、`job-vyU5zCLNa`）、或带 `?postId=` / `?jobId=` 等 ID 参数。
- **遇到以下形态，先别当详情页存**：站点主页（`domain.com/`、`/index`）、搜索 / 列表页（`/search`、`/s/`、`/list`、`/company`、`/gongsi`、`/zhaopin`、`?keyword=`）、无岗位 ID 的招聘平台路径、第三方资讯 / 工商站（脉脉、爱企查、新闻稿、小红书 / 抖音帖）。
- **只能拿到列表页时**：要么继续深挖到具体岗位详情 URL，要么如实存为「非详情页线索」并在 `match_points` 标注「⚠️ 此为列表页线索，需到官网搜岗位名核实」，绝不在 `link` 里塞一个主页 / 列表链接却伪装成具体岗位。
- 这条门禁和 L1 脚本判定共用同一套黑名单，AI 与脚本双重把关。

## 去重与报告（交给脚本，含链接验活闸门）

把检索得到的岗位 JSON 保存为 `jobs_raw.json`，运行 `python scripts/run.py` 一键完成：

**四步流水线，第 3 步是「链接验活闸门」——未通过就根本走不到报告：**

1. **需求就绪**：无 `prefs.json` → 先跑 `scripts/brief.py` 收集需求；无 `jobs_raw.json` → 提示把 AI 返回的 JSON 存为 `jobs_raw.json`。
2. **链接验活闸门（强制，不可绕过，两道关）**：`run.py` 出报告前调用 `scripts/verify_links.py --check`，**任一关不过直接中止，拒绝出报告**：
   - **关①（L1 脚本）**：未验活 / 验活超过 24 小时 / 仍有 `dead` 未清理 → 拦。
   - **关②（L2 AI 裁决）**：脚本判不了的 `unknown` 若**没有 `l2_status` 字段**（即 AI 还没亲自打开看过）→ 拦。
     这一关是「点开显示职位已下架」的最后防线：强反爬平台的死链脚本永远判 `unknown`，若 `unknown` 能无条件放行，等于没验。

   **标准四步（有 `unknown` 时必须走完）：**

   ```bash
   python scripts/run.py --verify              # ① 自动 L1 验活 + 清洗；若有 unknown 会在门禁处被拦下
   python scripts/verify_links.py --export-unknown   # ② 导出待裁决清单 verify_unknown.json
   #    ③ AI 逐条 WebFetch 打开，把每条的 l2_status 填成 alive / dead / unsure（+ l2_evidence 写页面原文证据），
   #       另存为 verify_l2.json
   python scripts/verify_links.py --import-l2  # ④ 回写裁决：dead 自动移除并备份，alive 升级为「AI已复核」
   #     ⚠️ --import-l2 严格按「link 主键」匹配，并交叉校验 company/title；不要用 idx 或「清单第几条」当键，
   #        对不上直接中止且不改数据（真实事故：用清单序号当键导致 29 条裁决整体错位、串岗，详见下方 L2 章节「回填铁律」）
   python scripts/run.py                       # 门禁放行，出报告
   ```

   - 若 AI 也打不开（BOSS 直聘这类强登录墙）→ 填 `unsure`，门禁放行，报告标「⚠️ 需手动复核」并附再检索线索、打水印。
   - 逃生阀：`python scripts/run.py --allow-unknown` 跳过关②（报告打「未确证」水印）；`--skip-verify` 跳过全部（打「未验活」水印）。**两者都会让死链漏给用户，非必要不用。**
3. **增量去重（只推变化）**：调用 `scripts/jobstore.py`，以岗位链接为主键、描述为辅助键，跨天 / 跨平台去重。识别四类变化事件：`new`（从没见过）/ `updated`（JD / 薪资 / 要求变了）/ `reopened`（曾疑似下架本次重现）/ `possibly_closed`（连续 3 次搜不到才疑似下架，一次不误判）。
4. **渲染报告**：调用 `scripts/report.py`，生成 `reports/report-*.xlsx`（**每行附真实可点击链接，第 4 列为「验活」状态**）与 `reports/report-*.html`（卡片含验活徽标；含未验活岗位时打水印）。

> 去重原理（链接归一化 + 描述哈希 + 增量事件）详见 `references/runbook.md`；`verify_links.py` 的四态判定、失效文案清单、URL 形态黑名单见 `references/platforms-cn.md` 第九节与脚本内常量。**这条闸门是代码级的，不是提示词约束——提示词会被偷懒绕过，代码闸门不会。**

## 质检复核（独立 subagent，强制）

搜索完成、初步分级后，**必须启动一个独立的质检 subagent**，在输出给用户之前做最后一轮把关。

> **优雅降级（严禁冒充）**：若当前运行环境不支持启动独立 subagent（如某些轻量 AI 客户端），主 agent **必须亲自按下方同一份清单逐条自检**，并在交付时明确说明「本次未启用独立质检 subagent，由主 agent 自检」。**禁止以「主 agent 自检」冒充「独立 subagent 质检」、禁止宣称「已通过质检」。** 不得以任何形式静默跳过质检。

### 为什么需要这一步

搜索阶段处理数据量大、平台多，AI 在批量处理时容易出现：标题看着像但实际不匹配的岗位被标成 🟢；靠猜打分；链接是 404 / 已下架却未验证；同一岗位换了标题没去重。质检 agent 只专注「验证匹配质量与链接真实性」，不受搜索上下文干扰。

### 质检 agent 的工作内容

**输入：** 初步分级后的全部岗位列表 + 用户简历核心画像（目标方向、核心技能、经验年限、硬性要求）

**逐条检查 🟢 高度匹配的岗位（全量检查）：**

1. 岗位标题 / 关键词是否与用户的目标方向直接相关？
2. 岗位要求的核心技能，用户简历里是否至少覆盖 60%？
3. 经验年限要求是否在用户的合理范围内（±2 年）？
4. 用户的硬性要求（城市、企业类型、双休等）是否满足？

不满足以上任意一条 → **降级到 🟡 或 🟠**，并标注降级原因。

**抽查 🟡 基本匹配的岗位（抽 20%-30%）：**

- 有没有被低估的好岗位？标题不太像但实际很匹配 → **升级到 🟢**。
- 有没有完全不相关的混进来了 → **降级到 🟠 或直接移除**。

### 链接验活（强制，全量，最容易翻车的一环）

> ⚠️ **血泪教训**：曾经只验 🟢、且在 🟡→🟢 升级后没补验活，结果用户随手点开报告里一条岗位就是「职位已下架」。报告里**每一条链接用户都可能点**，只验 🟢 等于没验。

**两层验活架构（提示词会被偷懒绕过，代码闸门不会）：**

- **L1 脚本层（机械、确定、全量、零 token）**：`scripts/verify_links.py` 对每条链接做「URL 形态 → HTTP 状态码 → 重定向落点 → 失效文案」四步判定，产出 `verify_status`（alive / dead / not_detail / unknown）。这是**不可绕过的闸门**——`run.py` 出报告前强制 `--check`，未验活直接中止。脚本判得了的（明确死链、列表页、第三方资讯页）直接处理，不消耗 AI。
- **L2 AI 层（语义、处理 `unknown`）**：脚本判不了的（反爬加密页 / 登录墙 / JS 渲染，强反爬平台几乎必然落此）导出到 `verify_unknown.json`，由本质检 subagent 用 WebFetch 的渲染 / 语义能力补刀验活。AI 只处理这一小撮，既省 token 又不被海量链接拖垮注意力。

> 一句话：**L1 用脚本挡住 80% 的坑且零成本，L2 用 AI 兜底剩下 20%。两者共用同一套四态判定（见 `verify_links.py` 与下方表格），结论写回 `verify_status` / `verify_evidence` 字段，最终在报告里可视化。**

**L2 范围（AI 验活，处理 L1 导出的 `unknown` 集合，不可缩水）：**

- **全量验活所有带链接的岗位**（🟢 / 🟡 / 🟠 都要），不是只验 🟢。
- **分级变更后必须补验活**：任何在质检中被升级（🟡→🟢）或改动过的岗位，都要重新走一遍验活，绝不能沿用旧结论。
- 岗位多时（>20 条）拆成多个并行 subagent 分片验活，每个 subagent 内部再 4-5 条一批并行 WebFetch，**禁止逐条串行**。
- 登录墙平台（BOSS/智联）AI 打不开 → 判 `unsure`，报告标「⚠️ 需手动复核」并附再检索线索，不强行判 alive/dead。

**分片时每个 subagent 必须原样带上 `verify_l2_prompt.md`（`--export-unknown` 自动生成），不许自己现编指令。**

> ⚠️ 真实事故（2026-08-02）：分三片验活时主 agent 给每片现编了指令，A 片漏写了「投递截止日期」规则，
> 结果**同样是「投递窗口已过期」，A 片判 `unsure`、B 片判 `dead`** —— 同一份报告里两套标准。
> 所以判定标准由脚本固化下发，分片只负责执行。

**L2 裁决怎么填（`verify_unknown.json` → `--import-l2`）：**

导出的每条含 `idx` / `link` / `company` / `title` / `why`，AI 逐条 WebFetch 后**在清单上原地补**
`l2_status` + `l2_evidence` 两个字段，再 `--import-l2` 回写：

| `l2_status` | 什么时候填 | 回写后果 |
|---|---|---|
| `alive` | 页面完整展示**该岗位**的 JD（岗位名 + 职责/要求可见），且投递窗口未过 | 升级为「✅ AI已复核」 |
| `dead` | 明确失效文案 / 404 / 跳兜底页无该岗位内容 / **页面写明的投递截止日期早于今天** | **自动移除**并备份到 `dropped_dead.json` |
| `unsure` | 落到登录/注册页、安全验证、页面空白——AI 也确认不了 | 保留但标「⚠️ 需手动复核」并附再检索线索，报告打水印 |

`l2_evidence` 必须写**页面上的原文证据**（如「显示『当前职位审核中或已下线』」「原文『网申时间：2026-04-18 ~ 2026-06-17』已过期」），不许写「我觉得应该还在」这类推测。

**回填铁律（违反会导致裁决张冠李戴）：**

1. **`link` 是唯一可靠主键，回填时绝不能删掉它。** 脚本 link 优先匹配，只有整份文件都没 link 时才退回 idx。
2. **清单里的 `idx` 是「岗位在数据中的下标」，不是「清单里的第几条」。** 这两个数在有岗位被 L1 清洗后就不相等。
3. 不要重排顺序、不要删条目、不要另建编号体系。**最稳的做法是直接在 `verify_unknown.json` 上原地加两个字段。**

> ⚠️ 真实事故（2026-08-02）：主 agent 给 subagent 编号时用了「清单第几条」（0-28），
> 而脚本按「数据下标」理解，结果 29 条裁决整体错位——**美团的岗位挂上了拼多多的验活证据，
> 商汤挂上了腾讯优图的**，还照此错删了 2 条好岗。
> 现在 `--import-l2` 会拿 `company` / `title` 做交叉校验，对不上直接中止且不改数据（自测已固化该 case）。

**L2 穿透力实测（2026-08 实跑，决定该抱多大期望）：**

| 平台 | L1 脚本 | L2 AI WebFetch | 说明 |
|---|---|---|---|
| 牛客网 / nowpick | ⏳ JS 渲染取不到正文 | ✅ **能拿到完整 JD** | L2 必能补上 |
| **51job / 前程无忧** | ⏳ WAF 返回密文 | ✅ **能穿透 WAF 拿到完整 JD** | 死链率最高的平台，L2 恰好治得了 |
| 实习僧 / 牛企直聘 / 应届生 | 多数可直判 | ✅ 补刀 | — |
| **BOSS 直聘 / 智联** | ⏳ 登录墙 | ❌ **只拿到登录/注册页** | 填 `unsure` → 报告标「⚠️ 需手动复核」+ 再检索线索，交付时明说需 App 内确认 |

> 结论：L1 + L2 之后，仍无法确证的只剩 BOSS / 智联这类强登录墙。**对这类必须在交付时明说「这 N 条需手动复核，请用报告里的再检索线索到平台 App 内确认在招」，不能装作都是活的。**

**四态判定（脚本 L1 与 AI L2 共用同一套，证据优先，严禁脑补）：**

| 判定 | 条件 | 处理 |
|---|---|---|
| `alive` | 页面正常展示该具体岗位的 JD（岗位名 + 职责/要求可见） | 保留，标注「✅ 已验活」 |
| `dead` | 出现明确失效文案或 404、或跳兜底页且无任何该岗位内容、或**页面写明的投递截止 / 网申结束日期早于今天**（窗口关了＝投不了，即使没有下架字样） | **直接移除**，摘要中计数并列出 |
| `not_detail` | 页面能开、无失效提示，但是公司主页 / 职位列表页 / 网站首页 / 第三方资讯帖（脉脉、爱企查、新闻稿） | **降级到 🟡**，标注「⚠️ 链接非具体岗位详情页，需到官网搜岗位名核实」 |
| `unknown` | 抓取失败 / 超时 / 登录墙 / 安全验证 / 内容为空 | 保留原级别，**但必须进 L2 由 AI 逐条裁决**——门禁不放行未裁决的 `unknown` |

**铁律：抓不到内容一律判 `unknown`，绝不能因为「抓不到」就判 `dead`。** BOSS 直聘、猎聘的安全验证墙极常见，误删会丢掉真实好岗。

**同样是铁律：`unknown` 不是终点，是待办。** 脚本判 `unknown` 只说明"脚本看不穿"，不代表岗位还在。必须交 L2 裁决成 `alive` / `dead` / `unsure` 才算完；`--check` 门禁会守住这条。

**各平台失效页特征文案（命中即 `dead`）：**

- **51job / 前程无忧**：「当前职位审核中或已下线」+ 跳空搜索状态图（`search_empty.png`）——**过期率最高的平台，必查**
- **实习僧**：职位描述上方标「当前职位已下线」（JD 快照仍可见，别被骗）
- **智联招聘**：「该职位已结束招聘」「职位不存在」
- **BOSS 直聘**：「职位已关闭」「该职位已停止招聘」
- 通用：404、「职位已下架」「已过期」「招聘已结束」、自动跳回列表/搜索页

**额外时效检查（`alive` 也要看）：**

页面虽正常展示，但 JD 里写的**投递截止日期 / 网申时间已过当前日期** → 保留但降级 🟡，标注「⚠️ 页面标注截止 YYYY-MM-DD 已过，窗口可能已关闭」。若页面同时写「截止 X 日」又写「长期开放滚动面试」等自相矛盾内容 → 保留原级别，但标注矛盾点提醒人工核实。

**去重复核：**

- 同一岗位链接（归一化后）只保留信息最全的一条。
- **同一公司 + 同一岗位名称但描述不同 → 视为不同岗位，必须保留多条**（绝不误并）。
- 不同平台的同一岗位 → 合并，来源平台标注多个。

**数据完整性检查：**

- 薪资、城市、公司名这些关键字段是否为空？空的标注「未标注」而不是留空白。
- 链接是否完整可点击？

### 质检结果

质检完成后，生成一份简短的质检摘要（内部使用，不展示给用户）：

```
质检摘要：
- 原始 🟢 18 个 → 质检后 🟢 14 个（4 个降级到 🟡）
- 原始 🟡 35 个 → 抽查 10 个，2 个升级到 🟢（已补做验活），1 个降级到 🟠
- 链接验活 L1（脚本，全量 75 条）：alive 52 / dead 3 / not_detail 9 / unknown 11
  - 移除已下架 3 个：<公司·岗位名·平台·失效文案>
  - 非详情页降级 9 个：<列出>
- 链接验活 L2（AI 逐条 WebFetch，处理上面 11 条 unknown）：alive 7 / dead 1 / unsure 3
  - AI 确认已下架再移除 1 个：<公司·岗位名·页面原文>
  - AI 也无法确证 3 个（BOSS/智联登录墙）：已标「⚠️ 需手动复核」+ 再检索线索，建议平台内搜索兜底
- 时效：2 个页面标注截止日期已过 → 降级并标注
- 去重：移除 3 个重复岗位
- 最终：🟢 16 / 🟡 32 / 🟠 24，共 71 个
```

**交付给用户时，必须主动说明三件事**（缺一不可）：

1. 移除了几条已下架岗位（L1 脚本判死 + L2 AI 判死，分开报）；
2. 几条是「⚠️ 需手动复核」（BOSS / 智联等登录墙），**明说这些 AI 无法确证在招、点开可能需登录**，建议用报告里的再检索线索到平台 App 内搜索确认；
3. 验活时间点——验活只是那一刻的快照，岗位随时可能下架，别让用户以为报告永久有效。

不要只报「共 N 条」就完事——用户点开一条死链，整份报告的可信度就没了。

质检通过后，再把结果交给 `scripts/report.py` 生成 Excel 与 HTML。

## 输出格式

报告生成后，按匹配度向用户说明结果：

- 🟢 高度匹配 / 🟡 基本匹配 / 🟠 可以尝试 三类分级
- 列出岗位标题、公司、城市、薪资、匹配点、链接
- **主交付物是 Excel 表格**（`reports/report-*.xlsx`，每行链接可点击）；HTML 报告作为网页版补充
- 新增为 0 时也要说明，不要静默结束

## 投递进度追踪

检索、投递之后，用 `scripts/jobstore.py` 的 `applications` 表管理个人投递流水（与「岗位是否在招」分表，互不干扰）：

- 记录：`python scripts/jobstore.py apply --url <岗位链接> --status 已投递 --note "8/1 投递"`
  - `status` 取值：`todo` / `筛选中` / `已投递` / `笔试` / `面试中` / `终面` / `Offer` / `拒信` / `放弃`
  - 链接会自动关联到 `jobs` 表，补全公司 / 岗位 / 城市；首次投递时间自动记录，后续改状态不会覆盖。
- 查看：`python scripts/jobstore.py applications`（全部）/ `app-stats`（按状态统计）

> 这是找实习真正用得上的「投递看板」。报告只负责「找岗」，投递进度请走这里。

## 推送与通知（便携，不绑死某家运行时）

报告生成后，可用 `scripts/send_notifications.py` 把「本期变化摘要 + Excel 附件」通过邮件 / 企业微信发出。**纯标准库，跟着仓库走，不依赖任何特定 AI 工具**。

- 配置（环境变量，建议放在项目根目录的 `.env`（不进仓库，分享包未含需自行创建）：`SMTP_HOST` / `SMTP_PORT` / `SMTP_USER` / `SMTP_PASS` / `TO_EMAIL` / `WECHAT_WEBHOOK`
- 发送：`python scripts/send_notifications.py --report reports/report-YYYY-MM-DD.xlsx --new-jobs new_jobs.json --channel all`
- 校验：`--dry-run` 只生成摘要、不真正发送
- 也可交给 WorkBuddy 自动化在每日检索后外推（二者可并存）

## 迭代与追加搜索

用户可要求：

- 「换个方向搜」→ 调整关键词重新搜索
- 「加上 XX 城市」→ 扩大范围
- 「再搜 BOSS 直聘」→ 直接 `site:zhipin.com ...` 公开快照检索，标「⚠️ 需手动复核」即可，无需任何登录流程。
- 「导出 / 打开报告」→ 打开 `reports/` 下的 Excel 或 HTML

## 模块协作

- `scripts/brief.py`：交互式问答，生成 `prefs.json` 与 `prompt.txt`（含简历与平台选择）
- `scripts/jobstore.py`：增量去重库（URL 主键 + 描述辅助键 + 增量事件 + 投递进度，纯标准库）
- `scripts/run.py`：串联 去重 → 报告
- `scripts/report.py`：渲染本地 Excel 表格（附链接、含变化/公司类型列、疑似下架分区）+ HTML 报告
- `scripts/send_notifications.py`：便携通知发送器（SMTP 邮件 + 企业微信，纯标准库）
- `templates/report.html`：HTML 报告模板
- `references/platforms-cn.md`：中国大陆全量平台注册表（含下架标志语）
- `references/recheck-platforms.md`：登录墙平台的公开快照检索与再检索指引（含再检索线索字段约定）
- `references/verified-official-sources.md`：已人工核验的官方招聘域名清单（季度复核）
- `references/company-classification.md`：公司分类规范（`company_tag` 标签枚举）
- `references/excel-export.md`：Excel 列结构与格式
- `references/runbook.md`：工具无关的执行手册

本 skill 的检索方法论受 [CareerForge job-hunt](https://github.com/rebecha1227-a11y/CareerForge) 的「如何搜岗位」思路启发，由作者参考该方法独立编写实现，并自行扩展出简历核心输入、平台确认门禁、独立质检与链接验活、精准去重与 Excel / HTML 报告等能力。

## 特殊场景

### 搜不到或很少

主动建议放宽城市、尝试相近岗位名、降低薪资要求；给出替代关键词让用户自己在招聘平台试试。

### 重复岗位

同一岗位在多个平台出现时，合并去重，保留信息最全的那条；同一公司同名不同描述的多个岗位**分别保留**。

### 登录墙平台（BOSS / 猎聘 / 智联 / 51job / 脉脉）

这些平台依赖登录态才能看完整 JD 与投递，但**公开快照可被 WebSearch 直接索引**。
做法：直接 `site:` 公开快照检索，搜到的公开结果纳入清单，逐条标注「⚠️ 需手动复核」并填全再检索线索
（岗位名 / 帖子标题 / 作者 / 发送时间 / 来源），由用户在自己已登录的 App 内确认在招与投递。
本 skill **不登录任何平台、不处理 cookies、不索要凭证**；登录墙链接的验活会落 `unsure`，报告统一标「⚠️ 需手动复核」。
详见 `references/recheck-platforms.md`。

### 持续追踪

本 skill 不内置自动定时搜索。需要再次检索时，随时重新运行 `python scripts/run.py`（或重新发起检索）。
由于 `jobstore.py` 记录每条岗位的「上次出现时间」与「连续缺失次数」，**再次运行只输出本期发生变化的部分**：
新增、JD 更新、重新开放、以及连续 3 次完整检索都搜不到的「疑似下架」，不会把历史全量重发。
