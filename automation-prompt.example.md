# 岗位雷达 · 自动化 Prompt（通用版）

本文件给「分享给他人」用。下面两段 prompt 已去掉原作者的机器路径和称呼，
换成占位符。复制时请替换：

- `{JOB_RADAR_DIR}`：job-radar 安装后的**绝对路径**
  - macOS / Linux： `~/.workbuddy/skills/job-radar`
  - Windows： `C:\Users\你的用户名\.workbuddy\skills\job-radar`
  - 同时把自动化任务的「工作目录(cwd)」也设为这个路径
- `{USER_NAME}`：你希望 assistant 怎么称呼你（如「广陵王」）
- `{DIRECTION}`：你的求职方向（如「AI产品运营实习」），仅用于日报标题

---

## 任务一：每日自动检索（建议 08:00）

> 在 WorkBuddy 自动化里新建「每日」任务，时间建议 08:00，cwd = `{JOB_RADAR_DIR}`。

读取并严格执行 {JOB_RADAR_DIR}/references/runbook.md 的 daily 模式全部步骤，完成今日的岗位检索、去重、质检、日报生成与推送。

关键约束：
1. 全程无人值守，禁止向用户提问。信息缺失时按 runbook 默认值处理，并记入日报「待补充」区。
2. daily 模式**对所有注册平台一律直接 `site:` 公开快照检索，不区分免登录 / 登录墙**（详见 references/platforms-cn.md 全表）——BOSS/猎聘/智联/51job/脉脉 等登录墙平台同样直接搜，搜到的公开结果统一标注「⚠️ 需手动复核」并附再检索线索（岗位名 / 帖子标题 / 作者 / 发送时间 / 来源），提示用户在对应 App 内确认在招再投递。**禁止任何需要登录态 / cookies 的方式**（本 skill 不登录任何平台）。
3. 必须执行 runbook 步骤 3 的 jobstore.py 去重脚本（以岗位链接为主键、描述为辅助键），只有脚本输出的新增岗位才能进日报。绝不推送库中已存在的岗位。
4. 同一家公司同名不同岗（描述不同）必须视为不同岗位，保留为多条。
5. 新增为 0 时也要发一条简讯说明，不要静默结束。
6. 搜索阶段控制在 10 分钟内，超时用已有结果继续。

推送要求：
7. 日报生成后，**必须通过微信 Claw 通道推送给 {USER_NAME}**。这是主推渠道（push-config.md 渠道一：微信，状态已启用）。
8. 推送内容为精简版日报，**必须用纯文本排版（禁用任何 Markdown 标记：不得出现 **、#、>、-、• 等符号；微信不渲染 Markdown）**。推荐格式：
   - 第1行：📡 岗位雷达 · M/D
   - 第2行：今日新增 N 个 {DIRECTION} 岗
   - 第3行：🟢 高度匹配 X　🟡 基本匹配 Y
   - 每条岗位前用「━━━━━━」分隔线
   - 每条结构：档位emoji+公司 / 岗位名·城市 / 薪资｜出勤 / (🟢才写)亮点：… / 链接单独一行写完整 https 裸URL（保证微信可点击）
   - 末尾「━━━━━━」后附：完整报告：reports/YYYY-MM-DD.html
   - 控制总长度适合手机单屏阅读，🟡 最多 15 条。
9. 同时保留本地日报文件到 {JOB_RADAR_DIR}/reports/YYYY-MM-DD.html 和 .xlsx。

---

## 关于"登录平台"的说明（无需单独任务）

本 skill **不登录任何平台、不处理 cookies、不要求用户先登录**。BOSS/猎聘/智联/51job/脉脉 等登录墙平台
的**公开快照仍可被 WebSearch 直接索引**，因此 daily 任务已经把它们一并搜了，结果标「⚠️ 需手动复核」并附再检索线索。
**所以不需要"每日登录提醒"任务**，登录墙平台的确认与投递由用户在自己已登录的 App 内完成。详见 `references/recheck-platforms.md`。
